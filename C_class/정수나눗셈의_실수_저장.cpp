#include <stdio.h>

void main()
{
    int a = 100, b = 200;    // 정수형 변수 a와 b에 값을 지정한다.
    float result;            // 실수형 변수 result를 선언한다.

    result = a / b;          // a를 b로 나눈 결과를 실수형 변수 result에 대입한다.

    printf("%f \n", result);
}
